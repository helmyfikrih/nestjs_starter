# Application
NODE_ENV=development
PORT=3000

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
USERNAME=dev_user
PASSWORD=dev_password
DB_NAME=nest_starter_dev

# JWT Configuration
JWT_SECRET=dev_jwt_secret_replace_in_production
JWT_EXPIRATION=30d
JWT_REFRESH_SECRET=dev_refresh_secret_replace_in_production
JWT_REFRESH_EXPIRATION=60d

# Throttling
THROTTLE_TTL=60
THROTTLE_LIMIT=10

# Swagger
SWAGGER_ENABLED=true